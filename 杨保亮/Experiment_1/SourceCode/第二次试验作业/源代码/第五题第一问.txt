#include <iostream.h>
#include"windows.h"
using namespace std;
add(int a, int b);

int main()
{
	int x, y, sum;
	cout << Please input x and y: << endl;
	cin >> x >> y;
	sum = add(x, y);
	cout << x << y << sum;
	system("pause");
	return 0;

}
int add(int a, int b)
{
	return a + b;
}